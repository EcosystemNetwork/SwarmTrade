services:
  swarmtrader:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    command: ["mock", "3600", "--web", "--dashboard"]
    restart: unless-stopped
